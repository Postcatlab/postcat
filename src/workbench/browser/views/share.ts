import {
  Form,
  Button,
  Input,
  Overview,
  Component,
  Element,
  Module,
  Title,
  Line,
  Text,
  Canvas,
  ModalS,
  WorkspaceS,
  HTTPS,
  MessageS,
} from '../elements';

export default new Module({
  id: 'share',
  children: [
    new Component({
      id: 'share',
      children: [],
    }),
  ],
});
